{
  "name": "portfolio-tracker-full",
  "version": "1.0.0",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ..",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "dependencies": {}
}
